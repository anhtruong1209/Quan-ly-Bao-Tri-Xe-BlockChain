{
  "name": "vehicle-warranty-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "install": "cd client && npm install && cd ../server && npm install"
  },
  "engines": {
    "node": "22.x"
  }
}
